package com.example.dynamicform.platform.common.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration for platform-level components including persistence, access layers, and versioning.
 */
@Configuration
public class PlatformPersistenceConfig {

    // Configuration class for platform components
    // ObjectMapper bean is provided by JacksonConfig in product.config package
}
